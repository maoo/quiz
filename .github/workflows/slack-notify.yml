# Disabled for now, because we don't have a Slack webhook
# name: Workflow Completion Notification

# on:
#   workflow_run:
#     workflows:
#       - "Test SVG to PDF"
#       - "Test SVG Generator"
#       - "Integration Test"
#     types:
#       - completed

# jobs:
#   notify:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Check workflow status
#         uses: actions/github-script@v6
#         with:
#           script: |
#             const workflow_run = context.payload.workflow_run;
#             const workflow_name = workflow_run.name;
#             const conclusion = workflow_run.conclusion;
#             const run_url = workflow_run.html_url;
            
#             console.log(`Workflow ${workflow_name} completed with status: ${conclusion}`);
#             console.log(`Run URL: ${run_url}`);
            
#             // You can add notifications here, like sending to Slack or other services
#             // For example:
#             /*
#             const slack_webhook = process.env.SLACK_WEBHOOK;
#             if (slack_webhook) {
#               await fetch(slack_webhook, {
#                 method: 'POST',
#                 headers: {'Content-Type': 'application/json'},
#                 body: JSON.stringify({
#                   text: `Workflow ${workflow_name} completed with status: ${conclusion}\nRun URL: ${run_url}`
#                 })
#               });
#             }
#             */